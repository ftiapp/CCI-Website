export const metadata = {
  title: 'CCI Admin Login - Climate Change Forum',
  description: 'Login page for CCI Climate Change Forum admin portal',
};

export default function LoginLayout({ children }) {
  return children;
}
